# Daily Note — 09 Apr 2026

## Session Handoff — 15:04

**Task:** Rimuovere tutto Vercel dal progetto + fix deploy admin/app.html

**Done:**
- `vercel.json` ELIMINATO dal repo ✅
- `deploy.yml` aggiornato: aggiunto `mirror dist/admin/ /public_html/admin/` prima del mirror generale (bypassa --only-newer per l'admin) ✅
- Password admin: salvata in `.admin-pwd` sul server (persiste tra deploy) ✅
- `_config.php`: legge `.admin-pwd` prima di `STATS_PASSWORD` env ✅
- `cambia-password.php` + `reset-password.php`: scrivono in `.admin-pwd` ✅
- Login screen: link "Password dimenticata?" aggiunto ✅
- Commit pushati: `003f15fc`, `a19bdad4`

**Remaining (URGENTE):**
- `src/pages/admin-panel.astro` riga 214-221: blocco Vercel da sostituire con GitHub Actions — edit bloccato da session pressure
  - Vecchio: `<a href="https://vercel.com/marcos-projects-7b8eef8c/marcomunich-com"...>Vercel</a>`
  - Nuovo: `<a href="https://github.com/consulenza-hash/marcomunich-astro/actions"...>GitHub Actions</a>`
- Commit + push dopo fix admin-panel.astro
- Aggiornare knowledge-base.md con regola HARD: "NIENTE VERCEL"
- Utente deve: disconnettere repo da Vercel su vercel.com + aggiornare URL repo GitHub (gear icon → marcomunich.com)

**Decisions:**
- Vercel completamente rimosso — deploy su Netsons via GitHub Actions + FTP lftp
- Password admin: `.admin-pwd` sul server Netsons (fuori dal mirror lftp, persiste tra deploy)
- Admin folder: sempre sovrascritta ad ogni deploy (mirror senza --only-newer)

**Files:**
- `D:/marcomunich.com/src/pages/admin-panel.astro` — riga 214, sostituire blocco Vercel (già letto offset 210-224)
- `D:/marcomunich.com/.github/workflows/deploy.yml` (modificato)
- `D:/marcomunich.com/public/api/_config.php` (modificato)
- `D:/marcomunich.com/public/api/cambia-password.php` (modificato)
- `D:/marcomunich.com/public/api/reset-password.php` (modificato)

**Next:** Edit `src/pages/admin-panel.astro` righe 214-221 — sostituisci blocco Vercel con GitHub Actions. Poi aggiorna `knowledge-base.md` con regola "NIENTE VERCEL — eliminare immediatamente qualsiasi riferimento". Poi commit + push tutto.

## Session Handoff — 15:33

**Task:** Scheduling 21 articoli in bozza (1/giorno) + fix deploy Netsons --delete

**Done:**
- 21 draft articles: date impostate Apr 9–29 (1/giorno, partendo da oggi) ✅
- `.github/workflows/auto-publish.yml` creato: cron 08:00 UTC, rimuove `bozza: true` quando `data <= oggi`, commit+push ✅
- `social-media-rivela-capacita...`: aggiunto campo `immagine` con placeholder (API Imagen scaduta) ✅
- Workflow auto-publish triggerato manualmente via GitHub API (HTTP 204) ✅
- Verificato live: tutti e 21 i draft sono 404 su marcomunich.com — nessun draft visibile ✅
- Ultimo articolo pubblico confermato: `/oltre-la-nicchia-brand-autentico-ossessione/` ✅
- Commits pushati: `e68b61d3`, `9d3cbf54`

**Remaining:**
- Aggiungere `--delete` al mirror lftp in `deploy.yml` (bloccato da session pressure)
  - File: `D:/marcomunich.com/.github/workflows/deploy.yml` riga 91
  - Change: `--only-newer \` → `--only-newer --delete \`

**Decisions:**
- `--delete` necessario: `lftp mirror --only-newer` è append-only, file rimossi da dist/ restano su Netsons → articoli cancellati resterebbero accessibili per sempre
- Imagen API key scaduta: placeholder image per ora, utente deve rigenerare chiave su Google AI Studio

**Files:**
- `D:/marcomunich.com/.github/workflows/deploy.yml` — riga 91, aggiungere --delete
- `D:/marcomunich.com/.github/workflows/auto-publish.yml` (creato)
- `D:/marcomunich.com/src/content/articoli/*/index.mdoc` — tutti 21 modificati (date)
- `D:/marcomunich.com/scripts/gen-single-image.py` (creato)

**Next:** Edit `deploy.yml` riga 91 — aggiungi `--delete` al mirror generale Netsons. Poi commit + push. Una riga sola.

## Session Handoff — 16:15

**Task:** DNS migration marcomunich.com → Cloudflare Pages + fix login persistente Playwright

**Done:**
- CORS _config.php: wildcard * → whitelist marcomunich.com ✅
- deploy.yml riscritto: 3 job separati (build / deploy-cf / deploy-api) ✅
  - CF Pages: sempre, ogni push
  - Netsons FTP: solo se public/api/** cambiata
- setup-dns.yml: confermato che marcomunich.com NON è in Cloudflare DNS (gestito da Netsons) ✅
- Commit a8d0fe16 pushato ✅

**Remaining:**
- DNS change su Netsons (il problema era che Playwright era rotto + Chrome extension non visibile)
- Configurare Playwright con profilo persistente (sessioni salvate tra restart)

**Decisions:**
- DNS di marcomunich.com è su Netsons registrar — non modificabile via CF API
- Playwright è il tool giusto MA serve profilo persistente per non riloggare ogni volta

**Next dopo restart:**
1. Trovare config Playwright MCP (probabilmente in .claude/settings.local.json o project settings)
2. Aggiungere --user-data-dir al comando Playwright MCP per sessioni persistenti
3. Usare Playwright per entrare su Netsons DNS e fare le modifiche

**Files:**
- D:/marcomunich.com/.github/workflows/deploy.yml (riscritto)
- D:/marcomunich.com/public/api/_config.php (CORS fixed)
- D:/marcomunich.com/.github/workflows/setup-dns.yml (diagnostic, da eliminare dopo)

## Session Reset Handoff — 16:25

**Objective:** Completare migrazione DNS marcomunich.com → Cloudflare Pages

**Completed:**
- deploy.yml: 3 job separati — CF Pages sempre, Netsons solo per API ✅
- CORS _config.php: wildcard → whitelist marcomunich.com ✅
- lftp --delete aggiunto ✅
- 21 draft schedulati Apr 9-29, auto-publish attivo ✅

**Remaining:**
- Aggiungere Playwright persistente a .mcp.json (--user-data-dir)
- DNS Netsons: puntare @ a CF Pages, aggiungere record api → 89.40.173.242
- Eliminare setup-dns.yml (diagnostico)

**Next action:** Apri .mcp.json, aggiungi entry "playwright" con --user-data-dir C:\Users\marco\AppData\Local\ms-playwright\mcp-persistent-profile, riavvia Claude Code, poi entra su Netsons DNS con Playwright.

**Read-first:** .claude/memory.md, .mcp.json, .github/workflows/deploy.yml

## Session Handoff — 17:40

**Task:** Verifica se @astrojs/react è ancora necessario dopo rimozione Keystatic
**Done:**
- Confermato: @astrojs/react già rimosso — commentato in astro.config.mjs riga 5, assente da package.json, zero file .jsx/.tsx nel sorgente
**Remaining:**
- Aggiornare Task Board.md: spuntare task @astrojs/react come done
- Google Cloud Project ID per Veo API — richiede input da Marco
- Aggiornare GitHub Secret STATS_PASSWORD → verificare login admin live — richiede azione su GitHub
**Decisions:**
- @astrojs/react non serve: zero dipendenze React attive, sicuro rimuoverlo definitivamente se mai installato
**Files:**
- D:\marcomunich.com\astro.config.mjs (read — react commentato riga 5)
- D:\marcomunich.com\package.json (read — nessuna ref a react)
- D:\marcomunich.com\Task Board.md (read)
- D:\marcomunich.com\.claude\memory.md (read)
**Refs:** nessuno
**Next:** Apri Task Board.md, segna @astrojs/react come done, poi chiedi a Marco se ha il Google Cloud Project ID o vuole procedere con STATS_PASSWORD GitHub secret

## Session Reset Handoff — 18:32

**Objective:** Abilitare pubblicazione automatica giornaliera reel (reel-02 oggi, poi in ordine numerico)

**Completed:**
- Analisi profilo Instagram @marcomunich1983 completa (337 post, 547 follower, formato vincente = smascherare meccanismi del settore)
- 5 opinioni impopolari scritte da articoli reali + filtrate anti-AI + accorciate
- Piano reel "opinion template" definito (testo overlay su video Marco fermo in camera)
- Infrastruttura publish_reel.py + publish-daily.yml verificata (cron attualmente disattivato)

**Remaining:**
- Creare `.claude/reels-status.json` con reel-01 marcato come pubblicato (media_id: "skipped")
- Creare `.github/workflows/publish-reels-daily.yml` — workflow solo reel, cron `0 8 * * *`
- Triggerare manualmente oggi per reel-02, poi automatico da domani
- [Futuro] Script per reel "opinion template" con video Marco + testo overlay FFmpeg

**Next action:** Scrivere `.claude/reels-status.json` poi `publish-reels-daily.yml`, poi commit + push + trigger manuale

**Read-first files:**
- `D:/marcomunich.com/.claude/memory.md`
- `D:/marcomunich.com/.github/workflows/publish-daily.yml`
- `D:/marcomunich.com/scripts/publish_reel.py`
- `D:/marcomunich.com/scripts/publish_daily.py`

## Session Reset Handoff — 19:28

**Objective:** Ripristinare sito marcomunich.com (down) cambiando DNS A record via cPanel API + deploy Netsons

**Completed:**
- reels-status.json (reel-01 skipped) + publish-reels-daily.yml creati e pushati ✅
- make_opinion_reels.py + opinions.json creati (FFmpeg opinion template) ✅
- Causa down diagnosticata: CF Pages apex "pending" — CNAME su apex impossibile con DNS Netsons ✅
- cPanel trovato: https://hostingweb21.netsons.net:2083 — auth con FTP_USERNAME:FTP_PASSWORD ✅
- Workflow add-cf-domain.yml esiste ma è la versione DIAGNOSTICA (non fa ancora il fix) ✅

**Remaining:**
- Riscrivere add-cf-domain.yml con: build + lftp full deploy + cPanel DNS fix
- Commit + push + trigger workflow
- Trigger publish-reels-daily per reel-02

**Next action:** Leggi add-cf-domain.yml (è diagnostico), riscrivilo con il workflow Fix DNS, push, trigger

**Spec workflow Fix DNS (add-cf-domain.yml):**
- Step 1: checkout + npm ci + npm run build
- Step 2: strip: find dist/contenuti-social/immagini-caroselli -name "*.png" -delete && find dist/contenuti-social/video-reels -type f -delete
- Step 3: lftp mirror dist/ → /public_html/ (full site) + api/ + admin/ su FTP 89.40.173.242
- Step 4: cPanel API Basic auth FTP_USERNAME:FTP_PASSWORD su hostingweb21.netsons.net:2083:
  parse_zone → trova line_index A records per @ → remove_zone_record → add_zone_record A 89.40.173.242 ttl=300

**Note importanti:**
- www.marcomunich.com funziona (200) — solo apex è down
- GitHub token in C:/Users/marco/.git-credentials per trigger workflow via API
- Trigger: POST api.github.com/repos/consulenza-hash/marcomunich-astro/actions/workflows/add-cf-domain.yml/dispatches

**Read-first files:**
- D:/marcomunich.com/.claude/memory.md
- D:/marcomunich.com/.github/workflows/add-cf-domain.yml
- D:/marcomunich.com/.github/workflows/deploy.yml

## Session Reset Handoff - 19:02
**Objective:** Nascondere articoli con data futura (> 9 aprile 2026) nel blog statico di marcomunich.com.

**Completed:**
- Sito marcomunich.com ripristinato: DNS fixato via Netsons client panel (Playwright API), rimossi CF IPs (188.114.96.7/97.7), aggiunto A record 89.40.173.242 — sito risponde 200 OK
- reels-status.json (reel-01 skipped) + publish-reels-daily.yml (cron 0 8 * * *) creati, pushati, funzionanti
- Identificata struttura articoli: filtro per `bozza: true` già presente in [slug].astro, blog/index.astro, rss.xml.ts
- 86 articoli con date 2026 (range: 2026-04-09 → 2026-07-20); oggi restano i 2 del 09-04, tutti gli altri vanno nascosti

**Remaining:**
- Aggiungere filtro `data > buildDate` in src/pages/[slug].astro (getStaticPaths, riga ~26)
- Aggiungere filtro `data > buildDate` in src/pages/blog/index.astro (filter(), riga ~37)
- Aggiungere filtro `data > buildDate` in src/pages/rss.xml.ts (dopo check bozza, riga ~30)
- Aggiungere GitHub Actions cron per rebuild giornaliero automatico (così articoli appaiono nella data giusta)
- Commit + push + verifica deploy

**Blocker:** Session pressure 101 chiamate — hook blocca Edit/Write

**Next action:** Aprire nuova chat, leggere i 3 file, applicare il filtro data in tutti e 3, creare cron workflow, commit+push

**Read-first files:**
- D:\marcomunich.com\src\pages\[slug].astro (righe 21-32)
- D:\marcomunich.com\src\pages\blog\index.astro (righe 23-38)
- D:\marcomunich.com\src\pages\rss.xml.ts (righe 21-42)

**Logica filtro da applicare (identica nei 3 file):**
In [slug].astro (regex su raw frontmatter):
```js
const today = new Date(); today.setHours(23,59,59,999);
// nel filter, dopo il check bozza:
const dateMatch = fmStr.match(/^data:\s*(\d{4}-\d{2}-\d{2})/m);
if (dateMatch && new Date(dateMatch[1] + 'T12:00:00') > today) return false;
```
In blog/index.astro (oggetti già parsati):
```js
.filter(p => !p.bozza && (!p.data || new Date(p.data + 'T12:00:00') <= new Date()))
```
In rss.xml.ts (dopo `if (fm.bozza === true) return null;`):
```js
if (fm.data && new Date(String(fm.data) + 'T12:00:00') > new Date()) return null;
```

## Session Reset Handoff — 20:xx

**Objective:** Comprimere immagini public/images/ (403MB, 612 file, max 12MB per jpg)

**Completed:**
- Fix filtro data futuro: jsYaml parsa date come oggetti Date, non stringhe → `instanceof Date ? toISOString().slice(0,10)` — commit pushato, 204 articoli visibili ✅
- Blog verificato: dev server localhost:4321, 204 articoli visibili ✅
- Diagnostica prestazioni: 612 immagini, 403MB totali, zero WebP, nessun CDN (Netsons) → causa lentezza confermata

**Remaining:**
- Comprimere tutte le immagini in public/images/ con sharp
- Script da creare: `scripts/compress-images.mjs`
- Logica: JPEG 82% qualità mozjpeg + max 1400px larghezza, PNG 80%, skip se risparmio < 50KB, dedup per posts/ e articoli/ (stessi file duplicati)
- Poi: commit + push (il deploy le porterà su Netsons)

**Script già scritto in sessione (blocca da SESSION PRESSURE) — riscrivilo:**
```js
// scripts/compress-images.mjs
// node --experimental-vm-modules scripts/compress-images.mjs
// oppure: node scripts/compress-images.mjs (se package.json ha type:module)
import sharp from 'sharp';
// Per ogni .jpg/.jpeg/.png in public/images/:
// 1. resize max 1400px width (withoutEnlargement)
// 2. jpeg({quality:82, mozjpeg:true}) o png({quality:80, compressionLevel:9})
// 3. scrivi solo se risparmio > 50KB
// 4. dedup per basename (posts/ e articoli/ hanno gli stessi file)
// Log: filename, MB prima → MB dopo, risparmio %
// Totale finale: X MB → Y MB (-Z MB)
```

**Next action:** Apri nuova chat, scrivi scripts/compress-images.mjs, eseguilo con `node scripts/compress-images.mjs`, poi commit + push

**Read-first:** .claude/memory.md, Daily Notes/040926.md (questo file)

## Task in coda — Showcase landing da /risorse/

**Obiettivo:** Creare `/siti-web-showcase/` — landing nascosta (non in nav), raggiungibile solo dal bottone "Scopri come lavoro →" in risorse.astro (riga 82).

**Cosa deve contenere:**
- Hero compatto: "Siti web costruiti su misura" + frase breve
- Portfolio grid: anteprima dei demo principali con link (coach, counselor, olistico, fotografi, ristoranti, ecc.) — guardare src/pages/portfolio/index.astro per i dati
- Come lavoro: sezione sintetica da src/pages/dev/come-lavoro.astro
- Case study: 3 case study olistici (caso-studio-coach, caso-studio-olistico, caso-studio-counselor)
- Prezzi: sezione prezzi da src/pages/dev/prezzi.astro
- CTA finale: WhatsApp + /dev/contatti

**Change risorse.astro riga 82:**
- Da: `href="/sviluppo-siti-web/"`
- A: `href="/siti-web-showcase/"`

**File da creare:** `src/pages/siti-web-showcase.astro`
**Design:** seguire Design System MM (Inter, mm-orange, mm-black, mm-section-dark/light, mm-btn-orange)
**Nav:** NON aggiungere al nav — pagina nascosta intenzionalmente
**Read-first:** src/pages/dev/index.astro, src/pages/portfolio/index.astro, src/pages/dev/prezzi.astro, src/pages/dev/come-lavoro.astro

## Session Reset Handoff — 20:45

**Objective:** Comprimere immagini public/images/ + creare landing /siti-web-showcase/

**Completed:**
- sharp verificato installato (ok)
- Script compress-images.mjs progettato completamente (bloccato da pressure prima del Write)
- Dati raccolti per showcase: portfolio (13 demo), case study (6), come lavoro (6 step), prezzi (3 piani)
- Identificate 2 occorrenze di `/sviluppo-siti-web/` in risorse.astro (righe 82 e 134)

**Remaining:**
- Scrivere `scripts/compress-images.mjs` (logica pronta: JPEG 82% mozjpeg max 1400px, PNG 80% compr9, skip <50KB, dedup basename)
- Eseguire lo script: `node scripts/compress-images.mjs`
- Creare `src/pages/siti-web-showcase.astro` (hero + portfolio grid + come lavoro + 3 case study olistici + prezzi + CTA)
- Aggiornare risorse.astro: `/sviluppo-siti-web/` → `/siti-web-showcase/` (righe 82 e 134)

**Blocker:** Session pressure 81 chiamate — hook blocca Write/Edit

**Next action:** Apri nuova chat, scrivi compress-images.mjs, eseguilo, poi crea siti-web-showcase.astro, poi aggiorna risorse.astro

**Read-first files:**
- D:\marcomunich.com\.claude\memory.md
- D:\marcomunich.com\Daily Notes\040926.md (questa sezione)
- D:\marcomunich.com\src\pages\dev\portfolio.astro (case study dati, già letto)
- D:\marcomunich.com\src\pages\dev\come-lavoro.astro (6 step, già letti)
- D:\marcomunich.com\src\pages\dev\prezzi.astro (3 piani €1.500/€2.500/€4.000, già letti)
- D:\marcomunich.com\src\pages\risorse.astro (righe 79-136, già lette)

**Script compress-images.mjs — logica completa:**
```js
// JPEG: sharp(file).resize({width:1400,withoutEnlargement:true}).jpeg({quality:82,mozjpeg:true})
// PNG:  sharp(file).resize(...).png({quality:80,compressionLevel:9})
// Skip se (sizeBefore - outputBuffer.length) < 50*1024
// Dedup: Map(basename → firstFilePath); se stesso basename già visto E stesso size → copia buffer del primo
// Walk ricorsivo public/images/ con readdir+stat
// Log per ogni file compresso: rel path, MB prima → MB dopo, % risparmio
// Totale finale: X MB → Y MB (-Z MB)
```

**Dati showcase già raccolti:**
- Portfolio grid: E-commerce, Corso Online, Sito Vetrina, Landing Page, Blog, Booking, Portfolio Creativo, Ristorante, SaaS, Non-Profit, Immobiliare, Wedding + link /portfolio/[slug]
- Case study olistici: Chiara Martini (coach, /portfolio/caso-studio-coach), Giulia Ferretti (counselor, /portfolio/caso-studio-counselor), Letizia Amato (olistico, /portfolio/caso-studio-olistico)
- Come lavoro: Ascolto → Progettazione → Sviluppo → Revisione → Online → Supporto
- Prezzi: Vetrina €1.500, Professionale €2.500 (consigliato), E-commerce Da €4.000 + Crescita Continua €120/mese
- Design: Design System MM (Inter, mm-orange, mm-black, mm-section-dark/light, mm-btn-orange)
- CTA: WhatsApp #25D366 + /dev/contatti

## Session Handoff — 23:17

**Task:** Fix sito lento — redirect apex marcomunich.com → www (Cloudflare Pages)

**Done:**
- compress-images.mjs eseguito: 143 img compresse, 358MB → 196MB (-162MB) ✅
- siti-web-showcase.astro creato: stile /dev (dark, #3B82F6, dot-grid, Lora h1) ✅
- risorse.astro: /sviluppo-siti-web/ → /siti-web-showcase/ (2 link) ✅
- Commit d32ce402 pushato ✅
- Diagnosi lentezza: apex marcomunich.com → 89.40.173.242 (Netsons diretto), www → CF Pages (veloce)
- Fix individuata: redirect .htaccess apex → www

**Remaining:**
- Aggiungere redirect in public/.htaccess (bloccato da session pressure a 52 chiamate)
  - Inserire PRIMA di "# Homepage → servi il file statico Astro":
    ```
    # Apex → www (Cloudflare Pages)
    RewriteCond %{HTTP_HOST} ^marcomunich\.com$ [NC]
    RewriteRule ^(.*)$ https://www.marcomunich.com/$1 [R=301,L]
    ```
- Commit + push .htaccess

**Decisions:**
- www.marcomunich.com → CF Pages (già attivo, veloce)
- marcomunich.com (apex) → Netsons 89.40.173.242 (lento, redirect fix)
- .htaccess redirect 301 apex→www: soluzione senza toccare DNS

**Files:**
- D:\marcomunich.com\public\.htaccess (da modificare — già letto, riga ~87 "# Homepage → servi il file statico Astro")
- D:\marcomunich.com\src\pages\siti-web-showcase.astro (creato)
- D:\marcomunich.com\src\pages\risorse.astro (modificato)
- D:\marcomunich.com\scripts\compress-images.mjs (creato)

**Next:** Edit public/.htaccess — inserisci il blocco RewriteCond/RewriteRule apex→www PRIMA di "# Homepage → servi il file statico Astro". Poi commit + push.

## End of Day Summary — 09 Apr 2026

**Accomplishments:**
- compress-images.mjs: 612 img scansionate, 143 compresse, 358MB → 196MB (-162MB)
- /siti-web-showcase/ creata: portfolio 12 demo, 3 case study olistici, 6-step process, prezzi — stile /dev (dark, #3B82F6, dot-grid hero, Lora)
- risorse.astro: 2 link /sviluppo-siti-web/ → /siti-web-showcase/
- .htaccess: redirect 301 apex→www — tutto il traffico ora passa per CF Pages CDN
- dev/contatti.astro: email ciao@ → consulenza@marcomunich.com
- 5 commit pushati su main

**Decisions:**
- Lentezza sito: causa apex→Netsons diretto; fix .htaccess senza toccare DNS
- siti-web-showcase stile /dev (non MM standard): richiesta esplicita utente

**Open items:**
- /sec-review da fare (codice modificato senza security pass)
- CF Pages env vars ancora da aggiungere (ADMIN_PASSWORD_HASH + ADMIN_SESSION_TOKEN)

**Tomorrow:**
1. /sec-review (priorità assoluta per regola)
2. GitHub Secret STATS_PASSWORD update
3. SEO: schema_faq su 27 articoli (GEO-008)
