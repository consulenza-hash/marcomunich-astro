# 031726 - Daily Work Log

## Decisions
- Abbandonato Keystatic CMS → sostituito con admin custom Astro
- Motivo: difficoltà integrazione AI in Keystatic (SPA React, architettura chiusa)
- Keystatic rimane installato ma non più nel hub admin

## Meetings & Conversations
- Discussione su Keystatic: user frustrato, ha valutato le opzioni, deciso per admin custom

## Notes
- Worktree issue: dev server gira da worktree, non main repo — file vanno scritti nel worktree
- STATS_PASSWORD ha \n trailer nel .env → fix .trim() applicato a tutti i file admin
- GitHub API + token vuoto → 401, fix header condizionale
- import.meta.glob as:'raw' + eager:true carica 201 .mdoc a build time senza GitHub API

## End of Day Summary
- Admin custom completo: /admin/articoli (lista 201 art.), /admin/edit/[slug] (editor AI), /api/ai-action (6 azioni)
- Keystatic di fatto sostituito — hub admin aggiornato
- Fix auth: .trim() su STATS_PASSWORD in tutti i file admin
- Commit non fatto (da fare prima del deploy)
- Da verificare: salvataggio manuale in edit/[slug] → /api/salva-bozza (data_override)
- Domani: test salvataggio, poi valutare rimozione Keystatic/React
