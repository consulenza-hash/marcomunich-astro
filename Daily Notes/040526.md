# 040526 — Daily Note

## Focus
Piano editoriale social 6 mesi + automazione pubblicazione Instagram.

## Accomplishments
- **Piano editoriale 6 mesi**: 52 caroselli (8-10 slide l'uno = 432 PNG generate) + archivio reels + planner CSV
  - `contenuti-social/` con mese-2..6, archivio caroselli/reels, immagini renderizzate
- **Automazione Instagram via Graph API**:
  - `scripts/build_schedule.py` → costruisce calendario pubblicazioni
  - `scripts/publish_carousel.py` → pubblica caroselli da schedule
  - `.github/workflows/publish-carousel-daily.yml` → cron giornaliero
  - `SETUP-INSTAGRAM-API.md` → guida setup

## Commits
- `8d02fe2b` feat: piano editoriale 6 mesi + 432 PNG caroselli (52 × 8-10 slide)
- `f21dd608` feat: automazione pubblicazione Instagram via Graph API

## Decisions
- Publish pipeline via GitHub Actions scheduled workflow (no server extra necessario)
- Immagini pre-renderizzate come PNG (no runtime generation)

## Open items carried forward
- Task board invariato: bozza "Content Marketing Olistico", STATS_PASSWORD secret, /sec-review
- Configurare secrets IG (access token, business account id) in GitHub — vedi SETUP-INSTAGRAM-API.md
- Verificare primo run cron dello workflow publish-carousel-daily

## Guardian check
- **design-guardian**: NOT NEEDED ✅ (nessuna UI/visual modificata oggi — solo script Python + workflow YAML + contenuti markdown)
- **security-guardian**: MISSED ⚠️ — scritti `scripts/publish_carousel.py` + `scripts/build_schedule.py` + `.github/workflows/publish-carousel-daily.yml` (gestiscono token IG Graph API e secrets GitHub), security pass non eseguito. Aggiunto come priorità domani.

## Incident log summary
- 5 failure `mv` su Windows con PERMISSION denied durante riorganizzazione `contenuti-social/_archivio-test/` — workaround mv file-per-file. Nominato a knowledge-nominations.
- 2 Read > 10k token falliti (file articoli di bozza) — rispetto della regola "mai Read interi" verificato.
- 1 soft-block `rm -rf` correttamente gestito via mv.

## Afternoon addendum — pipeline IG Graph API operativa end-to-end

**Contesto**: sessione di ~3 ore iniziata con carosello 01 da pubblicare urgente, terminata con pipeline completa automatizzata.

### Flussi completati nel pomeriggio
1. **Carosello 01 pubblicato** via Meta Business Suite (Playwright) su FB+IG — ordine slide corretto al 2° tentativo, caption incollata manualmente dall'utente (Lexical editor non coopera con fill programmatico). Alt texts saltati (verranno via API dal 02 in poi).
2. **Schedule arricchito**: `enrich_schedule.py` ha parsato i 6 file markdown dell'archivio caroselli e ha iniettato in `scripts/schedule.json` per ogni entry: `slide_texts` (array), `alt_texts` (array auto-generato con pattern cover/middle/signature), caption UTF-8 pulita. 52/52 entries.
3. **Token IG + permessi**: creata Meta Developer App `marcomunich-social` (App ID 35343200448604606) con prodotto Instagram via flow "Instagram API with Instagram Login". @marcomunich1983 aggiunto come Instagram Tester, invito accettato, token long-lived 60gg generato.
4. **Scoperta critica — PNG non accettato da IG**: API `graph.instagram.com/{user}/media` rifiuta PNG con error code 9004, richiede JPEG. `scripts/convert_slides_to_jpeg.py` ha convertito 432 slide a JPEG quality 92 progressive. Le PNG restano side-by-side come asset originali.
5. **Deploy JPEG**: esteso `php-hotfix.yml` a `public/contenuti-social/**` per deploy FTP parallelo ~5min. Verificato HTTP 200 dal sito live.
6. **Script migrato a Instagram Login endpoint**: `publish_carousel.py` ora usa `https://graph.instagram.com` (non `graph.facebook.com`), legge `INSTAGRAM_USER_ID` (con fallback backward-compat a `INSTAGRAM_BUSINESS_ACCOUNT_ID`), URL slide `.jpg` non `.png`. Publish ora **solo su Instagram** (no Facebook crosspost).
7. **Test E2E reale**: con token long-lived locale, create_item_container con JPEG live → container ID `17913289002360993` ricevuto, scade in 24h senza essere pubblicato (test safe).
8. **GitHub secrets configurati** via Claude in Chrome MCP: `META_ACCESS_TOKEN` (183 char) + `INSTAGRAM_USER_ID` (25745812921764074). Workflow cron `publish-carousel-daily.yml` aggiornato all'env var name nuovo.
9. **Workflow dry-run su GitHub Actions**: run manuale triggerato, completato Success in 27s, log conferma lettura corretta dello schedule + alt_texts + JPEG URLs.

### File sensitive creati (tutti gitignored)
- `.meta-app-info` — App ID, IG User ID, IG App Secret, metadata (**conservato**)
- `.meta-token-long-lived` — token Meta (**securely wiped dopo upload a GitHub secret**)

### Token rotation calendar
Il token scade ~04/06/2026. Rigenera dal dev portal Meta (1 click "Genera token" nel modal, copy+paste in GitHub secret `META_ACCESS_TOKEN`).

### Commits del pomeriggio
- `1860dd3e` feat: pipeline API Instagram pronta + 432 slide JPEG

## Tomorrow's priorities
1. **Verifica cron 07/04 09:30** — carosello 02 deve essere online su @marcomunich1983 entro le 09:35 CEST. Se fallisce, controllare log GitHub Actions workflow run.
2. **/sec-review** su nuovi script IG Graph API + AdminGuard + [slug].astro (MISSED x2, carried forward)
3. Continuare bozza "Content Marketing Olistico"

## End of Day Summary
- **Morning**: piano editoriale social 6 mesi completo (52 caroselli, 432 PNG), pipeline scaffolding.
- **Afternoon**: pipeline IG Graph API completamente operativa — token, schedule arricchito con alt_texts, JPEG convertite, script migrato al flow Instagram Login, GitHub secrets configurati, workflow dry-run validato sul cloud runner.
- **Stato**: dal 07/04 in poi, il cron giornaliero pubblica autonomamente 1 carosello/giorno su @marcomunich1983 (solo IG) fino al 52° il 26/05/2026.
- **Carried forward**: /sec-review (2x MISSED), STATS_PASSWORD secret, bozza libro CMO, token rotation intorno al 04/06.
